# Copy to .env (which is gitignored) and fill in your real values.

# Phase 1 — Promo copy generation (Claude).
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Phase 3 — Cross-posting via Postiz Cloud.
# Get a key at https://platform.postiz.com -> Settings -> Developers -> Public API
POSTIZ_API_KEY=
