title: Catch Up
blurb: The catch-up is grim because nobody wants to think about it, not because it is hard. Paste your bank or Stripe transactions, get them categorized into the right Schedule C buckets with the three transactions flagged that need human review and why.
tags:
  - accounting
  - bookkeeping
  - finance
  - product
donate: true
demo:
  setup:
    - { action: scroll_to, selector: "#root" }
  steps:
    - { action: wait, duration_s: 3 }
    - { action: click, selector: '[data-testid="try-sample"]' }
    - { action: wait, duration_s: 1.5 }
    - { action: scroll_to, selector: "#input-panel" }
    - { action: wait, duration_s: 1 }
    - { action: click, selector: '[data-testid="analyze-btn"]' }
    - { action: wait, duration_s: 3 }
    - { action: scroll_to, selector: '[data-testid="summary"]' }
    - { action: wait, duration_s: 7 }
